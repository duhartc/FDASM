int main (int argc , char * argv[]) {
  //Initialiser la struct
  InitialiserStruct();
  
  //Parser et remplir le tableau
  file = Reuters blablaba;
  ParseFichier(file);
  
  //Scinder aléatoirement en apprentissage et test 
  //apprentissage 52 500 et test 18203
  tabApprentissage = ...;
  tabTest = ...;
  ScinderCollection(struct, tabAprentissage, tabTest);

  //Estimation paramètre
  EstimationBernouillli();
  EstimationMultidim();
  
  //Calcul test
  int PBC[29];
  PBC[classe]= CalculTest(..);

  //Generaliser et boucler sur 20.
}
